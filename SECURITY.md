# Security Notes

This repository must not store live browser profiles, cookies, OAuth tokens, API keys, app passwords, downloaded client secrets, or generated session folders.

Use local `.env` files or platform secret storage for credentials. The `.gitignore` is configured to block common secret and browser-session files, but review changes before every push.

